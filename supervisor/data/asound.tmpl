# Default to PulseAudio

pcm.!default {
    type pulse
    hint {
        show on
        description "Default ALSA Output (Open Peer Power PulseAudio Sound Server)"
    }
}

ctl.!default {
    type pulse
}
